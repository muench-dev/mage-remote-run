# Custom Endpoint Plugin Example

This plugin demonstrates how to use the active connection to call a custom REST endpoint.

## Installation

1. Add this plugin to your `mage-remote-run` configuration.

## Usage

```bash
mage-remote-run custom-endpoint
```
